import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ExtensionAgreementForHelperTenantsPage } from './extension-agreement-for-helper-tenants.page';

describe('ExtensionAgreementForHelperTenantsPage', () => {
  let component: ExtensionAgreementForHelperTenantsPage;
  let fixture: ComponentFixture<ExtensionAgreementForHelperTenantsPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ExtensionAgreementForHelperTenantsPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
