import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FormForRequestMoveInOutPermitPage } from './form-for-request-move-in-out-permit.page';

describe('FormForRequestMoveInOutPermitPage', () => {
  let component: FormForRequestMoveInOutPermitPage;
  let fixture: ComponentFixture<FormForRequestMoveInOutPermitPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(FormForRequestMoveInOutPermitPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
