import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FormForRequestRegistrationPermitPage } from './form-for-request-registration-permit.page';

describe('FormForRequestRegistrationPermitPage', () => {
  let component: FormForRequestRegistrationPermitPage;
  let fixture: ComponentFixture<FormForRequestRegistrationPermitPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(FormForRequestRegistrationPermitPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
