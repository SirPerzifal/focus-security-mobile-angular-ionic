import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FormForRequestBibycleTagApplicationPage } from './form-for-request-bibycle-tag-application.page';

describe('FormForRequestBibycleTagApplicationPage', () => {
  let component: FormForRequestBibycleTagApplicationPage;
  let fixture: ComponentFixture<FormForRequestBibycleTagApplicationPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(FormForRequestBibycleTagApplicationPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
